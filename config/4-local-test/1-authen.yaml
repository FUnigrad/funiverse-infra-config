apiVersion: v1
kind: Namespace
metadata:
  name: share
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: authen-service
  namespace: share
spec:
  replicas: 1
  selector:
    matchLabels:
      type: service
      name: authen-service
  template:
    metadata:
      labels:
        type: service
        name: authen-service
    spec:
      containers:
      - name: jdk
        image: maven:3.8.7-eclipse-temurin-17
        ports:
        - containerPort: 80
        command:
        - "/bin/bash"
        - "-c"
        - "sleep 3600"
        volumeMounts:
        - name: volume-1
          mountPath: /workdir
      volumes:
      - name: volume-1
        hostPath:
          path: /home/ubuntu/9-funiverse-authen-service
---
apiVersion: v1
kind: Service
metadata:
  name: authen-service
  namespace: share
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    type: service
    name: authen-service

